import logger from '../utils/logger';

export const initializeVectorDB = async (): Promise<void> => {
  // Placeholder - will implement later
  logger.info('Vector DB initialization skipped (not implemented yet)');
};
